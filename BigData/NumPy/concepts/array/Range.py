# Generates values from start to just before stop, with a given step.

import numpy as np

arr = np.arange(0, 10, 2)
print(arr)

#--------------------------------------------------------------------------

# Generates num evenly spaced values from start to stop, inclusive.
# Step is Auto-calculated

arr = np.linspace(0, 1, 5)
print(arr) 

