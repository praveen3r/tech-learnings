CREATE TABLE AUTH (
	ID NUMERIC ,
	USERNAME VARCHAR NOT NULL,
	CREDENTIAL VARCHAR NOT NULL,
	CONSTRAINT pk_auth PRIMARY KEY (ID),
	CONSTRAINT uk_auth UNIQUE (USERNAME),
	CONSTRAINT fk_auth_id FOREIGN KEY (ID) REFERENCES DEMO_USER(ID) ON DELETE CASCADE
);