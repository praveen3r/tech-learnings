import { StyleSheet } from "react-native";

export default StyleSheet.create({
  footerContainer: {
    justifyContent: "center",
    alignItems: "flex-end",
  },
  footer: {
    marginRight: 40,
    fontSize: 15,
    color: "#ffffff",
    textAlign: "right",
  },
});
